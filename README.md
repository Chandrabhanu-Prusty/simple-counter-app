# 🌿 Simple Counter App

A simple and elegant **React Counter App** with a forest-inspired theme. It features a counter with increment, decrement, and reset functionality, along with a personalized greeting component.

---

## ✨ Features

- **Counter** — Increment, decrement, and reset a count value
- **Greeting** — Enter your name to receive a personalized greeting
- **Forest Theme** — Beautiful green-toned UI with a nature-inspired design

---

## 🛠️ Prerequisites

Make sure the following are installed on your system:

- [Node.js](https://nodejs.org/) (v16 or later recommended)
- [npm](https://www.npmjs.com/) (comes bundled with Node.js)
- [Git](https://git-scm.com/) (optional, for cloning)

---

## 🚀 Installation

1. **Clone the repository**

   ```bash
   git clone https://github.com/<your-username>/simple-counter-app.git
   cd simple-counter-app
   ```

2. **Navigate to the React app directory**

   ```bash
   cd simple-react-app
   ```

3. **Install dependencies**

   ```bash
   npm install
   ```

4. **Start the development server**

   ```bash
   npm start
   ```

5. Open your browser and visit **http://localhost:3000**

---

## 📁 File Structure

```
simple-counter-app/
├── simple-react-app/          # Main React application
│   ├── public/                # Static public assets
│   │   ├── favicon.ico
│   │   ├── index.html         # HTML entry point
│   │   ├── logo192.png
│   │   ├── logo512.png
│   │   ├── manifest.json
│   │   └── robots.txt
│   │
│   ├── src/                   # Application source code
│   │   ├── components/        # Reusable React components
│   │   │   ├── Counter.js     # Counter component (increment/decrement/reset)
│   │   │   └── Greeting.js    # Greeting component (personalized hello)
│   │   │
│   │   ├── App.js             # Root application component
│   │   ├── App.css            # App-level styles (forest theme)
│   │   ├── App.test.js        # App component tests
│   │   ├── index.js           # React DOM entry point
│   │   ├── index.css          # Global styles
│   │   ├── logo.svg           # React logo asset
│   │   ├── reportWebVitals.js # Performance monitoring
│   │   └── setupTests.js      # Test configuration
│   │
│   ├── .gitignore
│   ├── package.json           # Dependencies & scripts
│   └── package-lock.json
│
├── package.json               # Root-level package config
├── package-lock.json
└── README.md                  # ← You are here
```

---

## 📜 Available Scripts

Run these commands from inside the `simple-react-app/` directory:

| Command         | Description                             |
| --------------- | --------------------------------------- |
| `npm start`     | Runs the app in development mode        |
| `npm test`      | Launches the test runner                |
| `npm run build` | Builds the app for production           |
| `npm run eject` | Ejects CRA configuration (irreversible) |

---

## 🧰 Tech Stack

- **React** 19
- **Create React App** (react-scripts 5)
- **JavaScript** (ES6+)
- **CSS** (custom forest theme)

---

## 📄 License

This project is open-source and available for personal/educational use.
